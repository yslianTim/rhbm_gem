{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Debug + RelWithDebInfo + Release",
            "type": "process",
            "command": "cmake",
            "args": [
                "-P",
                "${workspaceFolder}/cmake/BuildAllPresets.cmake"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
